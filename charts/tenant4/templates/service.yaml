apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenant1.fullname" . }}
  labels:
    {{- include "tenant1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tenant1.selectorLabels" . | nindent 4 }}
