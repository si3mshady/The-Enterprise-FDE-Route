tenantId: tenant3
organizationName: "Tenant 3 Simulation Corp"
agentConfig:
  mode: "passive"
  targetCategory: "mixed"

service:
  type: ClusterIP   # required to fix the nil pointer error
  port: 80

httpRoute:
  enabled: false

ingress:
  enabled: false
